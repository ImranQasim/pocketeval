# pocketeval
Measuring what on-device AI actually does on real phones. Behaviour quality, output variance, thermal drift and availability, against a cloud baseline. Public golden set, raw data in the repo, reproducible by anyone with two handsets.
